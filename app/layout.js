export const metadata = {
  title: 'Pregnancy and Kidney - Comprehensive Presentation',
  description: 'Detailed multi-slide presentation on pregnancy and the kidney, with references to Davidson, Kumar, and Harrison\'s Internal Medicine.'
};

import './globals.css';

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
